可以做，而且和你现在的项目结构是能接上的。但这里要区分两件事：

> **“加一个登录页面”不难；“邮箱注册 + 安全登录 + 每个用户只能看到自己的历史运行记录”是一个真正的用户系统。**

结合你现在的 **Streamlit + RunStore + SQLite + reports/** 架构，我会把它评估为 **中等偏上的改造**，而不是简单 UI 功能。

### 最推荐的方案：Supabase Auth + PostgreSQL

你要求的是：

```text
邮箱注册
↓
邮箱验证
↓
邮箱登录
↓
用户身份确定
↓
创建项目/开始审议
↓
RunStore 产生运行记录
↓
记录 user_id
↓
“最近项目”只显示当前用户
↓
点击历史项目
↓
恢复对应报告
```

Supabase 本身支持邮箱+密码注册和登录，也支持邮箱确认；Python SDK 可以直接调用 `sign_up()` 和 `sign_in_with_password()`。([Supabase][1])

而且它的 Auth 和 PostgreSQL 是连在一起的，可以用 `auth.uid()` + Row Level Security（RLS）限制用户只能访问自己的数据。([Supabase][2])

---

## 真正需要改的是这 4 层

### ① 认证层

新增：

```text
登录
注册
退出登录
邮箱验证
```

例如：

```text
┌────────────────────────────┐
│      创想∞ AI创业委员会      │
│                            │
│  邮箱                       │
│  ┌──────────────────────┐  │
│  │ example@email.com    │  │
│  └──────────────────────┘  │
│                            │
│  密码                       │
│  ┌──────────────────────┐  │
│  │ •••••••••            │  │
│  └──────────────────────┘  │
│                            │
│       [ 登 录 ]             │
│                            │
│   还没有账号？立即注册       │
└────────────────────────────┘
```

这里**不要自己写密码加密、密码校验、验证码、邮件验证逻辑**。

交给 Supabase Auth。

---

### ② 用户身份层

现在你的系统里面有：

```text
RunStore
run_id
agent_runs
reports
```

但是缺一个东西：

```text
user_id
```

需要变成：

```text
User
  │
  ├── Run A
  │     ├── Agent 1
  │     ├── Agent 2
  │     └── final_report
  │
  ├── Run B
  │     ├── Agent 1
  │     └── final_report
  │
  └── Run C
```

于是：

```text
用户 A 登录
→ 只能看到 A 的 Run

用户 B 登录
→ 只能看到 B 的 Run
```

这个才是“我的历史”的核心。

---

## ③ 历史记录层

你现在已经有：

> RunStore + Resume + 加载已有报告

所以这部分其实**不用从零做**。

现在主要是把：

```text
RunStore
```

从：

```text
全局运行记录
```

变成：

```text
用户级运行记录
```

例如数据库增加：

```text
runs
────────────────────────────
id
user_id
project_name
idea
status
created_at
updated_at
final_verdict
report_path / report_content
```

然后：

```text
最近项目
```

查询：

```sql
WHERE user_id = 当前登录用户
ORDER BY created_at DESC
```

---

## ④ 数据库层反而是最大的变化

这里有一个特别重要的问题：

你现在使用 SQLite / 本地 `reports/`，**不适合直接作为线上多用户历史数据库**。

Streamlit Community Cloud 官方明确说明，本地文件存储并不保证持久化，平台可能删除本地数据。([Streamlit 文档][3])

所以：

```text
本地：
SQLite
reports/

线上多用户：
不应该继续依赖本地文件
```

否则会出现很危险的情况：

```text
用户 A 注册
→ 创建项目
→ 看到历史

过一段时间

用户 A 再登录
→ 历史没了
```

所以如果真的上线这个功能，我建议把**用户历史元数据和结果持久化迁移到 PostgreSQL**。

---

# 但是有一个 Streamlit 特有的问题

这个要特别注意。

Streamlit 现在原生支持用户认证，但官方的 `st.login()` 走的是 **OIDC 身份提供商**，例如 Google、Microsoft、Okta、Auth0，而不是你想要的这种“自己在页面里输入邮箱+密码注册”。([Streamlit 文档][4])

所以：

### 方案 A：Google 登录

非常简单：

```text
Google
↓
Streamlit st.login()
↓
st.user
↓
user_id
↓
历史
```

这是 Streamlit 官方支持的路线。([Streamlit 文档][4])

### 方案 B：邮箱 + 密码注册

就不能单纯靠：

```python
st.login()
```

解决。

需要：

```text
Supabase Auth
+
Streamlit 登录界面
+
Session 管理
+
Supabase Database
```

也就是说，**你真正想做的是 B，它比单纯 Google 登录明显复杂一些。**

Supabase 本身支持 email/password 注册、登录和邮箱确认，所以认证服务这一块不用你自己实现。([Supabase][2])

---

# 我建议你的最终架构

不要把它做成：

```text
frontend
   ↓
SQLite
```

而是：

```text
                   ┌──────────────┐
                   │ Supabase Auth│
                   │ 邮箱注册/登录 │
                   └──────┬───────┘
                          │
                       user_id
                          │
                          ▼
┌─────────────────────────────────────────┐
│          Streamlit / 创想∞              │
│                                         │
│ 首页                                     │
│ 最近项目                                 │
│ 委员会                                   │
│ 设置                                     │
│                                         │
│ 12 Agent / DAG / Orchestrator            │
│                                         │
└──────────────────┬──────────────────────┘
                   │
                   ▼
          ┌──────────────────┐
          │ Supabase Postgres│
          │                  │
          │ users            │
          │ projects         │
          │ runs             │
          │ reports         │
          └──────────────────┘
```

并且数据库启用 RLS：

```text
user A → 只能 SELECT user_id = A
user B → 只能 SELECT user_id = B
```

Supabase 官方的 RLS 就是为这种“用户只能访问自己的行”设计的。([Supabase][5])

---

# 对你这个项目来说，最关键的是“历史”怎么定义

我不建议一开始做非常复杂的用户中心。

第一版只需要：

### 登录后

侧边栏：

```text
👤 用户邮箱

首页
最近项目
委员会
设置

────────────

退出登录
```

### 最近项目

```text
最近项目

┌────────────────────────────┐
│ AI健身助手                 │
│ 2026-09-22 14:32            │
│ ● 已完成                    │
│                            │
│ 项目评审：暂缓              │
│                        查看 │
└────────────────────────────┘

┌────────────────────────────┐
│ 校园二手交易平台             │
│ 2026-09-21 21:14            │
│ ● 已完成                    │
│                            │
│ 项目评审：可进入修订          │
│                        查看 │
└────────────────────────────┘
```

点击以后直接：

```text
历史 Run
↓
原来的终审页面
```

你现在已经有的：

> 最近项目 + 加载报告 + Resume

反而可以直接成为这个功能的基础。

---

# 难度我会这样拆

| 功能                    | 难度   |
| --------------------- | ---- |
| 登录/注册页面 UI            | ★★   |
| 邮箱注册                  | ★★   |
| 邮箱密码登录                | ★★   |
| 邮箱验证                  | ★★☆  |
| 用户 Session            | ★★★  |
| Run 绑定 user_id        | ★★★  |
| 最近项目按用户过滤             | ★★☆  |
| PostgreSQL 持久化        | ★★★  |
| 用户数据隔离/RLS            | ★★★★ |
| Streamlit + Auth 状态处理 | ★★★★ |
| 不影响现有 12 Agent/DAG    | ★★★  |

**整体：★★★☆，属于可以做，但需要认真设计的功能。**

---

# 最重要的是：现在不要碰 Agent 核心

你这次加登录，应该严格限定在：

```text
新增：
auth/
user/
persistence/

修改：
frontend/app.py
frontend/ui_components.py
frontend/ui_helpers.py
```

然后把现有：

```text
Agent
Registry
DAG
Orchestrator
Validator
Repair
RunStore
LLM
```

尽量保持接口不变。

理想状态是：

```text
现在：

run(idea)
    ↓
RunStore

以后：

run(user_id, idea)
    ↓
RunStore
    ↓
Supabase
```

业务层甚至不需要知道“用户登录”这回事。

---

## 我还有一个很重要的建议

**比赛提交版和长期产品版要分开考虑。**

你当前比赛版已经：

> 在线可运行 + 12 Agent + DAG + 红队 + 终审 + 报告 + UI

这已经是一个完整作品。

加入用户系统以后，项目从：

> **Agent Demo**

开始向：

> **真正的 SaaS 产品**

靠近。

但同时也会新增：

```text
认证
密码
邮箱
Session
数据库
RLS
持久化
注册验证
异常登录
```

这些都会增加 Cloud 部署风险。

所以我不会建议你为了“多一个功能”直接把现有稳定版本整个改掉。

**更合理的架构是：先把 Auth 和用户历史做成独立的一层，不碰现有 Agent 核心，然后再把 `RunStore` 的持久化后端从本地 SQLite 扩展到 Supabase。**

这样你的项目会形成非常漂亮的一条技术链：

```text
用户
 ↓
邮箱注册 / 登录
 ↓
个人工作空间
 ↓
创业想法
 ↓
12 Agent 委员会
 ↓
DAG 协同
 ↓
红队质疑
 ↓
项目评审
 ↓
路演答辩
 ↓
项目诊断报告
 ↓
个人历史项目
 ↓
再次打开 / Resume / 二次审议
```

这实际上比单独加一个“登录按钮”有价值很多。

[1]: https://supabase.com/docs/reference/python/auth-signup?utm_source=chatgpt.com "Python: Create a new user | Supabase Docs"
[2]: https://supabase.com/docs/guides/auth?utm_source=chatgpt.com "Auth | Supabase Docs"
[3]: https://docs.streamlit.io/develop/concepts/connections/connecting-to-data?utm_source=chatgpt.com "Connecting to data - Streamlit Docs"
[4]: https://docs.streamlit.io/develop/concepts/connections/authentication?utm_source=chatgpt.com "User authentication and information - Streamlit Docs"
[5]: https://supabase.com/docs/guides/database/postgres/row-level-security?utm_source=chatgpt.com "Row Level Security | Supabase Docs"
